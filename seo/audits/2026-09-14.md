# SEO Audit — 2026-09-14

## 0. Previous iteration status — all merged, all live

All prior branches (`seo-improvements` through `seo-improvements-8`) confirmed
fully merged into `main` (zero unique commits vs `main` on any of them).
`main` and `origin/main` match. This iteration branches as `seo-improvements-9`.

## 1. Live verification of 2026-09-09 "Pending verification" list

| Item | Result |
|---|---|
| `/catalogue/` and `/catalogue/ai-styles.html` serve corrected redirect/canonical/meta tags | **Confirmed live.** `curl -I /catalogue/` → `308` → `Location: /catalogue/ai-styles.html`. `ai-styles.html` has self-canonical, meta description, OG/Twitter tags all present and correct. `videos.html` unchanged/correct. |
| `/software/` indexing status in GSC | **Still not checked** — see §2, GSC access is now blocked by an account mismatch, a new/different failure mode than "no login session". |
| Fresh PageSpeed Insights mobile score | **Not re-attempted this run** — no new reason to think the public no-key API quota (`quota_limit_value: "0"`, confirmed structural in the 2026-09-09 audit) has changed. Still needs Jeff to run it manually or supply a key. |
| Directory submissions (Singapore Wedding Vendors, Bridestory, Venuerific) live | **Not found live** — not re-searched this run (no new submissions reported since 2026-09-10). See §4. |

## 2. New finding: Google Search Console access is blocked by account mismatch

Attempted to pull fresh GSC Performance data per the playbook's standing
instruction ("also pull search performance data if accessible"). Navigating
to the Performance report for `https://playingwithphoto.com/` in the
available browser session returned:

> "Oops, you don't have access to this property"
> Signed in as: jefflai0315@gmail.com
> Property: https://playingwithphoto.com/

This is a different failure mode from earlier iterations (which had no
browser session at all). The browser session here is authenticated as
**jefflai0315@gmail.com**, and that specific Google account does not have
access to the GSC property — meaning the account that verified GSC
(2026-07-21, per the playbook) is a different Google account than
jefflai0315@gmail.com, or access was never granted to this one.

**Needs Jeff**: either (a) grant `jefflai0315@gmail.com` access to the
`playingwithphoto.com` GSC property (Search Console → Settings → Users and
permissions → Add user), or (b) confirm which account does have access so
future automated sessions can be pointed at the right one. Until then, GSC
performance data can only enter this loop when Jeff pastes it in manually.

## 3. GBP reviews — live-checked, one new review + an attribution fix

Checked `business.google.com/reviews` directly (same `jefflai0315@gmail.com`
session — this one *does* have GBP access, unlike GSC). Findings:

- **3 reviews total** (up from 2 confirmed live as of 2026-09-10):
  1. **Patricia Chin** — 5 days ago — wedding review ("such a great
     experience with Jeff and the photobooth at our wedding..."). Reply
     already posted 2026-09-10.
  2. **Christine Chew** — 2 weeks ago — Teacher's Day/superhero review
     ("Great experience with Playing with Photo Photo Booth for an
     event..."). Reply already posted 2026-09-10.
  3. **Jaslynn Lai** — 4 days ago — **new, unreplied.** Daughter's 7th
     birthday party, "kids really enjoyed it..." — full text couldn't be
     captured; the GBP dashboard tab stopped responding to further
     automated reads partway through this check (repeated `document_idle`
     timeouts) before the truncated review could be expanded. A draft reply
     is queued in `seo/gbp-posts-queue.md` based on the available snippet —
     Jeff should skim the full review before posting in case it mentions
     something the draft misses.

- **Attribution correction**: earlier drafts (`gbp-posts-queue.md` and
  `testimonial-cards-proposal.md`, both written 2026-09-10) had the two
  original reviewers' names swapped — labeled Christine Chew as the wedding
  reviewer, when she's actually the Teacher's Day/superhero one (Patricia
  Chin is the wedding reviewer). The reply *content* was posted to the
  correct review in both cases either way (it was matched by story, not by
  name), so nothing needs to be redone on GBP itself — only the tracking
  files were wrong. Both corrected this iteration, along with the
  `testimonial-cards-proposal.md` card drafts (previously placeholder
  `[Name TBD]`, now the confirmed real names).

No new GBP Posts confirmed posted this run (didn't ask Jeff directly — no
user present in this scheduled run). Queue in `seo/gbp-posts-queue.md`
unchanged, still 5 drafts + 1 new reply awaiting Jeff's action.

## 4. Outreach + directory tracking — could not check this run

Gmail access (`search_threads`) returned "This connector requires
additional permissions" — could not check for replies to the Bridestory
email (sent 2026-09-10) or confirm whether SingaporeBrides / The Wedding
Vow / Venuerific outreach has been sent yet. `seo/outreach-drafts.md`
carried forward unchanged. Jeff: if you've sent any of the pending drafts
or heard back from Bridestory, update the checkboxes in that file (or just
tell the next session) so this doesn't get re-asked.

## 5. Technical health re-check — no regressions

- `robots.txt`: `Allow: /`, correct sitemap line. OK.
- `sitemap.xml`: HTTP 200, valid XML, 4 URLs, all matching live canonical
  URLs (`/`, `/software/`, `/catalogue/ai-styles.html`,
  `/catalogue/videos.html`). OK.
- Legacy noindex pages: `/Landing/` (404, still carries `X-Robots-Tag:
  noindex`) and `/snapshot/` (200, `X-Robots-Tag: noindex`) — both correct.
- Domain canonicalization: `www.playingwithphoto.com` → 308 →
  `playingwithphoto.com`. Still correct.
- Homepage title/meta unchanged and correct: `AI Photo Booth Singapore —
  Vintage Prints & Painter Styles | Playing With Photo` / meta description
  present.
- JSON-LD: homepage's 2 blocks (`LocalBusiness`, `FAQPage`) both parse as
  valid JSON. Catalogue and `/software/` pages have no JSON-LD (unchanged,
  no action needed — see 2026-09-09 audit's reasoning on why
  `SoftwareApplication` schema was deliberately not added to `/software/`).
- `vercel.json` and `sitemap.xml` both re-validated (JSON parse, XML parse)
  after this iteration's edits.
- All internal paths checked return expected status: `/` 200, `/software/`
  200, `/catalogue/` 308, `/catalogue/ai-styles.html` 200,
  `/catalogue/videos.html` 200, `/sitemap.xml` 200, `/robots.txt` 200.
- No new non-SEO commits landed on `main` since the 2026-09-09 audit (`git
  log --since=2026-09-09` shows only this loop's own SEO commits) — no new
  pages to audit this time, unlike the `/catalogue/` surprise last
  iteration.

Nothing broken, nothing new to fix on the technical side.

## 6. Backlog / structured-data note

`aggregateRating` for the homepage `LocalBusiness` JSON-LD (recurring
checklist item 5) remains blocked: now there are 3 reviews instead of 1,
but I could not re-confirm the current aggregate star rating from the GBP
profile page itself (the same dashboard responsiveness issue from §3 cut
that check short) — only the individual review list, which doesn't show
per-review star counts in the text-only read. The 2026-09-06 confirmation
was "5.0★, 102 customer interactions" at 1 review; with 2 more since, the
aggregate could plausibly still be 5.0 but that needs re-confirming, not
assumed. **Still holding off** until Jeff (or a future session with a
working dashboard view) confirms the current star rating and count
directly.

## Pending verification (next iteration)

- [ ] `/software/` indexing status in GSC — blocked on account access (§2).
- [ ] Fresh PageSpeed Insights mobile score — needs Jeff to run manually or
      share an API key.
- [ ] Directory submissions (Singapore Wedding Vendors, Bridestory,
      Venuerific) — re-check once Jeff confirms.
- [ ] Jaslynn Lai's GBP review reply — confirm posted once Jeff has
      reviewed the draft.
- [ ] Current GBP aggregate star rating/count, to unblock `aggregateRating`
      schema (§6).

## Needs Jeff

1. **GSC account access** — grant `jefflai0315@gmail.com` access to the
   `playingwithphoto.com` Search Console property, or confirm the right
   account, so future iterations can pull Performance data automatically
   instead of needing a manual paste. This is the single highest-value
   unblock right now — it's been the loop's biggest stated blocker since
   iteration 6, and this iteration found the *specific* reason it's still
   blocked.
2. **Merge `seo-improvements-9`** — low-risk, tracking-file-only changes
   (corrected review attribution, new review reply draft). No site-code
   changes this iteration.
3. **Reply to Jaslynn Lai's review** on GBP — draft in
   `seo/gbp-posts-queue.md`, please skim her full review first (see §3).
4. **Testimonial cards proposal** (`seo/testimonial-cards-proposal.md`) —
   attribution question now resolved; only remaining decision is
   photo-or-no-photo for the corkboard cards.
5. **GBP Posts queue** — still 5 unposted drafts in
   `seo/gbp-posts-queue.md`; post ~1 every 1-2 weeks.
6. **Outreach drafts** — SingaporeBrides, The Wedding Vow, Venuerific still
   unsent per `seo/outreach-drafts.md`; Bridestory sent 2026-09-10,
   reply status unknown (Gmail check blocked this run, §4).
7. PageSpeed Insights mobile score — still needs a manual run or API key.
